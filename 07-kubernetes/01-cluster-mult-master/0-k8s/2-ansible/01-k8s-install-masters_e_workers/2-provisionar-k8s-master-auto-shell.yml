- hosts:
  - ec2-k8s-m2
  - ec2-k8s-m3
  become: yes
  tasks:
    - name: "Reset cluster"
      shell: "kubeadm reset -f"

    - name: "Fazendo join kubernetes master"
      shell: kubeadm join k8s-haproxy:6443 --token yvts7j.y26edcqt1y2aarmq  --discovery-token-ca-cert-hash sha256:db0169647d3f2482c6869a978c0772d9b5ecbd426d602e409aba36771c879229  --control-plane --certificate-key 89c050f5397de605984ed43e2323b4172e9ef160373d2add9e82ef033ec4be1a

    - name: "Colocando no path da maquina o conf do kubernetes"
      shell: mkdir -p /home/ubuntu/.kube && sudo cp -f /etc/kubernetes/admin.conf /home/ubuntu/.kube/config && sudo chown 1000:1000 /home/ubuntu/.kube/config && export KUBECONFIG=/etc/kubernetes/admin.conf
#---
- hosts:
  - ec2-k8s-w1
  - ec2-k8s-w2
  - ec2-k8s-w3
  become: yes
  tasks:
    - name: "Reset cluster"
      shell: "kubeadm reset -f"

    - name: "Fazendo join kubernetes worker"
      shell: kubeadm join k8s-haproxy:6443 --token yvts7j.y26edcqt1y2aarmq  --discovery-token-ca-cert-hash sha256:db0169647d3f2482c6869a978c0772d9b5ecbd426d602e409aba36771c879229 

#---
- hosts:
  - ec2-k8s-m1
  become: yes
  tasks:
    - name: "Configura weavenet para reconhecer os nós master e workers"
      shell: kubectl apply -f "https://cloud.weave.works/k8s/net?k8s-version=$(kubectl version | base64 | tr -d '\n')"

    - name: Espera 30 segundos
      wait_for: timeout=30

    - shell: kubectl get nodes -o wide
      register: ps
    - debug:
        msg: " '{{ ps.stdout_lines }}' "
